node_modules
dist
dist-ssr
coverage
*.local

.env
.env.*
!.env.example
# Committed deliberately: development-only flags, no secrets. Vite reads it for
# `npm run dev` only, never for a production build.
!.env.development

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.vscode/*
!.vscode/extensions.json
.idea

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Supabase local stack
supabase/.branches
supabase/.temp

# Local PostgreSQL test clusters (scripts/test-rls.sh, scripts/perf-dataset.sql)
pgdata/
*-pgdata/

# TypeScript incremental build state (regenerated on every build)
*.tsbuildinfo
